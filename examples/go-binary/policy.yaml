# DEPRECATED: policy.yaml is no longer used.
#
# Policy configuration is now in provenix.yaml under the `policy:` key.
# This file is kept only for reference — it will be ignored by Provenix.
#
# See: examples/go-binary/provenix.yaml
